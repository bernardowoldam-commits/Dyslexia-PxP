# Entre Linhas

Front-end completo em Next.js + React + TypeScript + Tailwind CSS.

## Executar
npm install
npm run dev

## Build
npm run build

A aplicação usa dados mockados e não contém chaves de API. A arquitetura da interface está preparada para futura integração com autenticação, banco, OpenAI e geração de PDF.
